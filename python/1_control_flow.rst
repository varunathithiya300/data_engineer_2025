1. When you call a function, Python jumps into it and runs its code.
2. If that function calls another, Python pauses the first and runs the second.
3. Once a function returns, Python goes back to where it left off and continues.